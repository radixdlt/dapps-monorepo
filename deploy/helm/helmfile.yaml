helmDefaults:
  verify: false
  wait: false
  timeout: 600
  recreatePods: false
  force: false
  createNamespace: false
environments:
  dev: {}
  PR: {}
releases:
  - name: dashboard
    chart: ./rdxworks-dashboard
    values:
      - environments/{{ .Environment.Name }}/values.yaml.gotmpl